import 'package:flutter/material.dart';

class AppColors {
  // A professional slight blue tint for the answer view background
  static const Color answerBackground = Color(0xFFd4eefb);

  // You can add more colors here as the app grows, e.g. primary colors, text colors
  static const Color primary = Colors.blue;
}
